server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: smart-employment-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ???????????????????
    url: jdbc:mysql://localhost:3306/smart_employment?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_id